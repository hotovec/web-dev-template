<div class="m-filter">
    <form action="">
        <div class="l-left">

            <ul class="m-filter--items">
                <li class="label">
                    <div>vyber</div>
                </li>
                <li>
                    <select name="timepass" class="ch-select" data-placeholder="Vyber si">
                        {% for opt in 1..20 %}
                            <option value="val{{ opt }}">Option {{ opt }}</option>
                        {% endfor %}
                    </select>
                </li>
                <li>
                    <select name="timepass" class="ch-select">
                        {% for opt in 1..3 %}
                            <option value="val{{ opt }}" class="category">xOption {{ opt }}</option>
                            {% for opt in 1..3 %}
                                <option value="val{{ opt }}">Option {{ opt }}</option>
                            {% endfor %}
                        {% endfor %}
                    </select>
                </li>

            </ul>
        </div>

        <div class="l-right">
            <ul class=" m-filter--items">
                <li>
                    <select name="timepass" class="ch-select">
                        {% for opt in 1..20 %}
                            <option value="val{{ opt }}">rok {{ opt }}</option>
                        {% endfor %}
                    </select>
                </li>
            </ul>
        </div>

    </form>
</div>

